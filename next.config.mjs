/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['firebasestorage.googleapis.com','image.pollinations.ai','placehold.co', 'unsplash.com', 'images.unsplash.com']
    },
};

export default nextConfig;
