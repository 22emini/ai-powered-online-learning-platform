/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['firebasestorage.googleapis.com','image.pollinations.ai','placehold.co']
    },
};

export default nextConfig;
